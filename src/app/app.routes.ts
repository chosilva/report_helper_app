import { Routes } from '@angular/router';
import { ReportHelperComponent } from './report-helper/report-helper.component';

export const routes: Routes = [
  { path: '', component: ReportHelperComponent },
];
